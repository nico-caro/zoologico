{% extends 'base.html.twig' %}

{% block titulo %} Modificar Tipos {% endblock titulo %}

{% block contenido %}

<h1>Edicion de tipos</h1>
        <form action="actualizar" method="POST">
        <table>
            <tr>
                <td>
                    <label>Nombre </label>
                </td>
                <td>
                    <input type="hidden" name="txtId" id="txtId" value="{{ t.getId() }}" />
                    <input type="text" name="txtNombre" id="txtNombre" required minlength="2" maxlength="50" value="{{ t.getNombre() }}">
                </td>
            </tr> 
            <tr>
                <td>
                    <label>Descripcion </label>
                </td>
                <td>
                    <input type="text" name="txtDescripcion" id="txtDescripcion" required minlength="2" maxlength="50" value="{{ t.getDescripcion() }}">
                </td>
            </tr>

            <tr>
                <td></td>
                <td>
                    <input type="submit" value="Modificar" name="btnModificar" id="btnModificar">
                </td>
            </tr>
        </table>
         
        </form>
        <h4>{{ mensaje }}</h4>


{% endblock contenido %}